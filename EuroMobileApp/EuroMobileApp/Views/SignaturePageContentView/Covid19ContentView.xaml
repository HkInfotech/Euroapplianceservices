<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="EuroMobileApp.Views.SignaturePageContentView.Covid19ContentView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Margin="5,10,0,10">
    <ContentView.Content>
        <StackLayout BindableLayout.ItemsSource="{Binding covid19FormModels}" VerticalOptions="StartAndExpand">
            <BindableLayout.ItemTemplate>
                <DataTemplate>

                    <Grid ColumnDefinitions="*,Auto">
                        <Label
                            Grid.Column="0"
                            FontFamily="{StaticResource RobotoMedium}"
                            FontSize="Medium"
                            LineBreakMode="WordWrap"
                            Text="{Binding QuestionTitle}"
                            VerticalOptions="Start" />
                        <CheckBox
                            Grid.Column="1"
                            IsChecked="{Binding QuestionAnswer}"
                            VerticalOptions="Start" />
                    </Grid>

                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </ContentView.Content>
</ContentView>